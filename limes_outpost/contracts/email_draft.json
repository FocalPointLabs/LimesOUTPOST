{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "description": "DraftAgent output contract. Validates the email draft returned by _generate_draft() before enqueueing.",
  "type": "object",
  "required": ["subject", "body"],
  "properties": {
    "subject": {
      "type": "string",
      "minLength": 1,
      "maxLength": 200,
      "description": "Email reply subject line."
    },
    "body": {
      "type": "string",
      "minLength": 50,
      "description": "Full plain-text reply body. Must be at least 50 chars — guards against empty or truncated LLM output."
    }
  }
}